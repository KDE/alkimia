@PACKAGE_INIT@

find_package(KDE4 REQUIRED)
find_package(GMP REQUIRED)

include("${CMAKE_CURRENT_LIST_DIR}/LibAlkimiaTargets.cmake")

set_and_check( LIBALKIMIA_INCLUDE_DIR "@INCLUDE_INSTALL_DIR@")

set( LIBALKIMIA_LIBRARIES
  Alkimia::alkimia
  ${QT_QTCORE_LIBRARY}
  ${QT_QTDBUS_LIBRARY}
  ${GMP_LIBRARIES}
)

message(STATUS "Found LibAlkimia: ${CMAKE_CURRENT_LIST_DIR}/LibAlkimiaConfig.cmake (found version \"@ALKIMIA_LIB_VERSION@\")")
